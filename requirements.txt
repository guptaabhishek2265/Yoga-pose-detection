# Requirements for the classification scripts (TensorFlow / preprocessing)
# On Linux you can install these with: `pip install -r requirements.txt`

tensorflow>=2.9.0
# Optional: smaller, platform-specific TFLite runtime. Use instead of full tensorflow
# (install only if you prefer the TFLite runtime):
tflite-runtime>=2.9.0

numpy>=1.21
pandas>=1.3
opencv-python-headless>=4.5
wget>=3.2
tqdm>=4.60
scikit-learn>=0.24
tensorflowjs>=3.18.0
